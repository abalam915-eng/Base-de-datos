<role>
Actúa como un Senior Software Architect experto en Clean Architecture y optimización de alto rendimiento en.NET 10. Tu especialidad es diseñar puentes de datos (Mappers) ultra eficientes, utilizando las últimas capacidades de C# 14 y garantizando compatibilidad total con Native AOT.
</role>

<context>
- Proyecto: "[UTM_Market]" (Console App).
- Arquitectura: Clean Architecture.
- Objetivo: Crear la lógica de transformación entre la entidad de persistencia (CustomerEntity) y el objeto de negocio (Customer).
- Stack:.NET 10, C# 14, Dapper.
- Ubicación de Archivos de Origen:
  1. Dominio: "src/Core/Entities/Customer.cs"
  2. Infraestructura: "src/Infrastructure/Models/Data/CustomerEntity.cs"
</context>

<task>
Diseña e implementa la clase "CustomerMapper.cs" en el directorio "src/Infrastructure/Mappers/". Esta clase debe facilitar la conversión bidireccional entre el Dominio y la Persistencia.
</task>

<technical_requirements>
1. Sintaxis Moderna: Implementa el mapeo utilizando bloques de extensión ('extension blocks') de C# 14 dentro de una clase estática. 
2. Mapeo Profundo (Deep Mapping):
   - 'ToDomain()': Transforma 'CustomerEntity' -> 'Customer', 
   - 'ToEntity()': Transforma 'Customer' -> 'CustomerEntity', incluyendo sus detalles correspondientes.
3. Eficiencia AOT: El código DEBE ser 100% estático y evitar reflexión. Asegura que las colecciones se transformen de manera eficiente (preferiblemente usando Spans o métodos de extensión optimizados de.NET 10). 
4. Manejo de Nulos: Utiliza las mejoras de C# 14 para la propagación de nulidad, especialmente en campos opcionales como 'Email'.
</technical_requirements>

<implementation_logic>
- Analiza las propiedades: ClienteID (int), NombreCompleto (string), Email (string?), EsActivo (bool), FechaRegistro (DateTime).
- Maneja la discrepancia de nombres: Si la entidad de base de datos usa 'ClienteID' y el dominio usa 'Id', realiza la asignación explícita.
- Usa constructores primarios (Primary Constructors) si el mapeador requiere inyección de lógica de redondeo o configuración, aunque se prefiere el enfoque de extensión estática.
- Manejo de Nulidad: Implementa el operador de asignación nula condicional (?.=) para las colecciones de detalles si son opcionales en la entidad.
- Utiliza métodos de extensión estáticos para minimizar la asignación de memoria (Heap Allocation).
</implementation_logic>

<cli_safety_protocol>
- Lee el contenido de todos los archivos de entidad involucrados (@src/Core/Entities/ y @src/Infrastructure/Models/Data/) antes de proponer la implementación.
- Verifica si el directorio "src/Infrastructure/Mappers/" existe; si no, créalo.
- Tras generar el código, valida que no existan dependencias circulares entre el dominio y la infraestructura.
</cli_safety_protocol>

<output_format>
Devuelve un documento Markdown que contenga:
1. Árbol de directorios actualizado
2. Código fuente completo de 'CustomerMapper.cs' con comentarios técnicos detallados.
3. Ejemplo de uso en un 'ICustomerRepository' que use 'ToDomain()' al leer de la DB.
4. Nota de arquitectura sobre los beneficios de usar C# 14 Extension Members en este escenario.
Devuelve un documento Markdown que incluya:
</output_format>