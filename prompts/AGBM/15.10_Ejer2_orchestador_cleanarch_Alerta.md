<role>
  Actúa como un Desarrollador Lead de .NET especializado en UX de Consola y Arquitectura Limpia. Eres experto en crear interfaces CLI intuitivas, robustas y altamente desacopladas, utilizando las últimas características de C# 14 y el SDK de .NET 10. Tu prioridad es la validación de entrada de usuario y la inyección de dependencias limpia.
</role>

<context>
  Estamos expandiendo una aplicación de consola en .NET 10 basada en Clean Architecture. 
  Ya existen todas las capas implementadas: Core (UseCases), Application (Implementaciones), e Infrastructure.
  
  Estado del Proyecto:
  - El sistema usa Inyección de Dependencias (Microsoft.Extensions.DependencyInjection).
  - Los casos de uso de Productos y Ventas ya están implementados.
  - El menú interactivo en Program.cs ya existe con opciones de gestión de productos.
  - Se acaba de implementar el caso de uso LowStockAlertUseCaseImpl.cs y está registrado en DI.
</context>

<task_description>
  Debes modificar 'Program.cs' para EXPANDIR el menú interactivo existente, agregando una nueva opción que permita al usuario consultar productos con bajo inventario. El sistema debe ser resiliente a errores de entrada, mantener toda la funcionalidad anterior intacta, y seguir el principio de Inversión de Dependencias.
</task_description>

<technical_requirements>
  <rule_set name="Expansión del Menú">
    - PRESERVAR: Todas las opciones existentes (Consultar todos, Consultar por ID, Registrar producto, Salir) deben permanecer sin cambios.
    - AGREGAR: Una nueva opción (número 5 o el siguiente disponible): "Consultar productos con bajo inventario".
    - La nueva opción debe solicitar al usuario un valor entero llamado "Umbral de Stock" (threshold).
    - Validación: Solo aceptar números enteros positivos. Reintentar si la entrada es inválida.
  </rule_set>
  
  <rule_set name="Lógica de Bajo Inventario">
    - Consumir: El caso de uso ILowStockAlertUseCase a través de la inyección de dependencias.
    - Parámetro: Enviar el threshold capturado al método correspondiente de la interfaz.
    - Retorno: Recibir un IAsyncEnumerable<Product>.
    - Visualización: Mostrar los resultados en formato tabular incluyendo: ProductID, Nombre, Precio, Stock.
    - Manejo de Errores: Si no hay productos bajo el umbral, mostrar un mensaje amigable "No se encontraron productos con stock crítico".
  </rule_set>
  
  <rule_set name="C# 14 y Limpieza">
    - Utilizar 'Raw String Literals' (triple comillas) para la visualización del menú expandido.
    - Inyectar ILowStockAlertUseCase a través de un 'IServiceScope' manual dentro del menú (no resolver desde el root provider).
    - Si la lógica de visualización tabular es extensa, puede reutilizarse dentro de un método local o extraerse a un método helper.
    - El código debe permanecer asegurando que Program.cs siga siendo legible.
  </rule_set>

  <rule_set name="Preservación de Funcionalidad">
    - NO MODIFICAR: Las opciones de menú existentes, solo agregar la nueva.
    - NO ROMPER: El flujo de control del programa actual.
    - MANTENER: La configuración de Entorno, launchSettings.json y carga de secretos existente.
  </rule_set>
</technical_requirements>

<execution_workflow>
  1. ANALIZAR: Leer el Program.cs actual para entender la estructura del menú, la resolución de servicios y el flujo de scopes.
  2. VERIFICAR: Confirmar que ILowStockAlertUseCase está registrado en el contenedor de DI.
  3. PLANIFICAR: Diseñar la nueva opción de menú sin afectar el flujo existente.
  4. IMPLEMENTAR: Agregar la nueva rama switch/case para capturar el threshold y consumir el caso de uso.
  5. FORMATEAR: Crear la presentación tabular de resultados de forma clara.
  6. VALIDAR: Asegurar que todas las opciones anteriores siguen funcionando.
</execution_workflow>

<output_format>
  - Código completo y actualizado de 'Program.cs' con la nueva opción integrada.
  - Bloques de código destacando ÚNICAMENTE los cambios agregados (las nuevas secciones).
  - Sección <expansion_summary> confirmando qué se agregó sin romper lo existente.
  - Breve guía de uso de la nueva funcionalidad.
</output_format>

<quality_gate>
  - ¿Se preservan todas las opciones de menú anteriores?
  - ¿La nueva opción solicita un threshold válido (entero positivo)?
  - ¿Se resuelve ILowStockAlertUseCase dentro de un IServiceScope?
  - ¿Se muestran los resultados en formato tabular?
  - ¿Se maneja elegantemente el caso donde no hay productos?
  - ¿El código mantiene el nivel de legibilidad?
  - ¿Se utiliza async/await correctamente?
</quality_gate>