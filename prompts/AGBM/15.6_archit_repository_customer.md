<role>
Actúa como un Principal Software Architect experto en.NET 10, C# 14 y diseño de sistemas de alto rendimiento. Tu especialidad es la implementación de patrones de persistencia en Arquitecturas Limpias, optimizados para Native AOT y Dapper.
</role>

<context>
- Proyecto: "[UTM_Market]" (Console Application).
- Objetivo: Diseñar el contrato de repositorio (Interface) para la entidad "Customer".
- Stack Técnico:.NET 10.0 (LTS), C# 14, Native AOT.
- Mapeo: Se requiere compatibilidad total para una futura implementación manual vía SqlDataReader (Sin reflexión).
- Archivos de referencia: @src/Core/Entities/Customer.cs y @src/Infrastructure/Models/Data/CustomerEntity.cs
</context>

<task>
Crea la interfaz "ICustomerRepository.cs" en el directorio "src/Core/Repositories/". El contrato debe ser riguroso, estar en inglés y seguir los estándares de C# 14.
</task>

<interface_specifications>
1. Búsqueda por Identidad (Email):
   - 'GetByEmailAsync': Debe retornar 'Task<Customer?>'. Es crítico manejar la nulidad explícitamente con C# 14 NRT para evitar excepciones en el flujo de negocio si el cliente no existe.
2. Registro de Entidad:
   - 'AddAsync': Debe recibir el objeto de dominio 'Customer' y retornar el objeto persistido (con ID generado si aplica).
3. Consulta de Flujo (AOT Friendly):
   - 'GetAllAsync': Debe retornar 'IAsyncEnumerable<Customer>' para permitir el streaming de datos directamente desde el SqlDataReader sin buffering en memoria.
4. Cancelación Cooperativa:
   - TODOS los métodos asíncronos DEBEN incluir un 'CancellationToken cancellationToken = default' como parámetro final.
</interface_specifications>

<coding_standards_csharp14>
- Abstracción: La interfaz debe ser pura; no debe tener dependencias de infraestructura ni de tipos de Dapper.
- C# 14 Syntax: Si el método requiere un objeto de criterios de búsqueda pequeño, utiliza Primary Constructors en su definición. 
- IMPORTANTE: NO declares campos privados manuales (ej. _startDate) si usas 'field', ya que el compilador genera el respaldo automáticamente.
- Documentación: Incluye comentarios XML (///) que describan las precondiciones y postcondiciones de cada operación.
</coding_standards_csharp14>

<cli_safety_protocol>
</cli_safety_protocol>
- Lee el contenido de los archivos de dominio 'Customer' antes de generar el código para asegurar la coincidencia de tipos (ej. Email como string, IsActive como bool).
- Verifica si el directorio "src/core/repositories/" existe; si no, créalo.
- Notifica si el diseño del contrato sugiere alguna restricción para el campo 'Email' (como unicidad) que deba ser validada en la capa de aplicación.
</cli_safety_protocol>

</output_format>
Devuelve un documento Markdown que contenga:
1. El código fuente completo de 'ICustomerRepository.cs'.
2. Una breve sección de "Asesoría Arquitectónica" sobre el impacto de Native AOT en la futura implementación de este contrato.
</output_format>