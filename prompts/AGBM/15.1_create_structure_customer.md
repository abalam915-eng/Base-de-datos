<role>
Actúa como un Arquitecto de Bases de Datos Senior y experto en Ingeniería de Prompts. Tu objetivo es generar un script SQL de alta calidad y asegurar su persistencia en el sistema de archivos del usuario utilizando tus herramientas de edición de código y asi extender la base de datos "[demo_utm_AGBM]" para soportar el nuevo módulo de lealtad de clientes.
</role>

<context>
- Motor de DB: Microsoft SQL Server 2022 Express (Considera el límite de 10GB y optimiza tipos de datos).
- Base de Datos: "[demo_utm_AGBM]" (La base de datos ya existe, asegúrate de usarla).
- Ubicación de Salida: "/db/scripts/03_create_structure_customer.sql".
</context>

<task>
1. Genera un script DDL completo para la tabla Cliente.
2. Define tipos de datos precisos: usa DECIMAL(19,4) para valores monetarios y NVARCHAR para textos que requieran soporte Unicode.
3. Establece Identificadores Únicos (Primary Keys) usando INT IDENTITY para eficiencia (si es necesario, sino no lo agregues).
4. Implementa Relaciones (Foreign Keys) explícitas que reflejen la lógica de negocio 1:N solicitada.
5. Relación Futura: Un Cliente puede tener N Ventas (1:N).
6. Agrega restricciones CHECK para validar que precios, cantidades y stock no sean negativos, validar el Email contenga al menos un símbolo '@' y un punto '.', reforzando la lógica de la aplicación.
</task>

<requirements>
Crea la siguiente tabla con sus respectivas columnas y restricciones:

1. Tabla "Clientes":
    - Columnas: ClienteID (INT IDENTITY PK), NombreCompleto (NVARCHAR(150)), Email (VARCHAR(100) UNIQUE), EsActivo (BIT, por defecto 1), FechaRegistro (DATETIME, por defecto fecha actual).
    - Restricción: Agrega un CHECK para validar que el Email contenga al menos un símbolo '@' y un punto '.', reforzando la lógica de la aplicación.
    - Evolucion: Modifica la tabla "Venta" existente (del primer ejercicio) para agregar una columna ClienteID (FK) que permita saber qué cliente hizo la compra.

Asegura que el script sea idempotente usando "IF NOT EXISTS" o verificaciones previas de objetos.
</requirements>

<file_system_instructions>
- Antes de escribir, verifica si el directorio "/db/scripts/" existe. Si no, créalo.
- Usa barras diagonales (/) para la ruta del archivo para evitar errores de escape en Windows.
- Guarda el contenido generado en el archivo especificado.
- Una vez guardado, confirma la operación y resume las características de seguridad e integridad implementadas en el script.
</file_system_instructions>

<sql_best_practices>
- Incluye SET NOCOUNT ON y SET XACT_ABORT ON al inicio del script.
- Usa comentarios detallados que expliquen la arquitectura de la tabla.
- Asegura que el script sea idempotente (puedas ejecutarlo varias veces sin errores).
- Usa comentarios para explicar la relación entre esta tabla y la clase Customer.cs de .NET.
</sql_best_practices>
